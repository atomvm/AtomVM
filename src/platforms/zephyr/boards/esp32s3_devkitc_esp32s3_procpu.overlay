/*
 * Keep the Zephyr and AtomVM console on the ESP32-S3 native USB
 * Serial/JTAG port used for flashing and monitoring.
 */

/ {
	chosen {
		zephyr,console = &usb_serial;
		zephyr,shell-uart = &usb_serial;
	};
};

&usb_serial {
	status = "okay";
};

/*
 * Keep QEMU's RAM size within the virtual address space configured by the
 * board. This also prevents CONFIG_SYS_HEAP_AUTO from sizing the libc arena
 * from QEMU's larger physical-memory default.
 */
&dram0 {
	reg = <0x0 0x7fff00>;
};
